# tougu-bond-parser

Render deployment for the bond text recognition page.

## Local run

```bash
pip install -r requirements.txt
python3 app.py
```

## Deploy

This repository is intended to be deployed on Render using `render.yaml`.
